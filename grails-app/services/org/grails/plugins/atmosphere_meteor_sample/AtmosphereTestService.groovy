package org.grails.plugins.atmosphere_meteor_sample

class AtmosphereTestService {

	def recordChat(data) {
		// This service could be used to persist chat messages to a data store.
		println "AtmosphereTestService.recordChat: ${data}"
	}
}
